{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "this is node server for my chat app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "arpit",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.5",
    "socket.io": "^2.3.0"
  }
}
